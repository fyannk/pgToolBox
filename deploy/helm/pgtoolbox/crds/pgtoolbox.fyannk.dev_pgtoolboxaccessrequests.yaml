---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pgtoolboxaccessrequests.pgtoolbox.fyannk.dev
spec:
  group: pgtoolbox.fyannk.dev
  names:
    kind: PgToolBoxAccessRequest
    listKind: PgToolBoxAccessRequestList
    plural: pgtoolboxaccessrequests
    shortNames:
    - pgreq
    singular: pgtoolboxaccessrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.pgConsoleRef.name
      name: Console
      type: string
    - jsonPath: .spec.subject
      name: Subject
      type: string
    - jsonPath: .status.requestedRoleRef.name
      name: Role
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgToolBoxAccessRequest describes one request for access to a
          PgConsole.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PgToolBoxAccessRequestSpec is the desired state of an access request,
              created by the proxy when an authenticated identity is unknown to the
              console. The proxy service account holds only create permission.
            properties:
              message:
                description: Free-form justification from the requester, shown to
                  the reviewer.
                maxLength: 1024
                type: string
              pgConsoleRef:
                description: The PgConsole the requester wants access to, in the same
                  namespace.
                properties:
                  name:
                    description: The name of the referenced resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              subject:
                description: |-
                  The identity the authentication provider vouched for, set by the
                  proxy. Immutable: a request belongs to exactly one identity.
                maxLength: 253
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: subject is immutable
                  rule: self == oldSelf
            required:
            - pgConsoleRef
            - subject
            type: object
          status:
            description: |-
              PgToolBoxAccessRequestStatus is the observed state of an access request.
              The review decision — state, requestedRoleRef, decidedBy, decidedAt — is
              written by a dba-level user through the console, and only the status
              subresource, so the create-only proxy cannot self-approve.
            properties:
              conditions:
                description: 'Standard conditions: Decided, UserReady.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              decidedAt:
                description: When the decision was taken.
                format: date-time
                type: string
              decidedBy:
                description: The identity of the reviewer who took the decision.
                type: string
              observedGeneration:
                description: The generation observed by the controller.
                format: int64
                type: integer
              requestedRoleRef:
                description: |-
                  The PgToolBoxRole the reviewer granted, required when state is
                  approved. The operator materializes the PgToolBoxUser from it.
                properties:
                  name:
                    description: The name of the referenced resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              state:
                default: pending
                description: The decision state.
                enum:
                - pending
                - approved
                - denied
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
