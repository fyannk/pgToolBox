---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pgtoolboxusers.pgtoolbox.fyannk.dev
spec:
  group: pgtoolbox.fyannk.dev
  names:
    kind: PgToolBoxUser
    listKind: PgToolBoxUserList
    plural: pgtoolboxusers
    shortNames:
    - pguser
    singular: pgtoolboxuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.pgConsoleRef.name
      name: Console
      type: string
    - jsonPath: .spec.subject
      name: Subject
      type: string
    - jsonPath: .spec.roleRef.name
      name: Role
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgToolBoxUser describes one identity on one PgConsole.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PgToolBoxUserSpec is the desired state of a user: one identity on one
              console. Deleting a PgToolBoxUser de-provisions it from the proxy
              configuration and from pgAdmin.
            properties:
              localPasswordSecretRef:
                description: |-
                  Reference to the Secret holding this user's local-mode password
                  (bcrypt). Only meaningful when the console's proxy authentication
                  mode is local. Default key: "password".
                properties:
                  key:
                    description: The key inside the Secret.
                    type: string
                  name:
                    description: The name of the referenced Secret.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              pgConsoleRef:
                description: The PgConsole this user belongs to, in the same namespace.
                properties:
                  name:
                    description: The name of the referenced resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              roleRef:
                description: |-
                  The PgToolBoxRole granting this user its level and postgres role,
                  in the same namespace.
                properties:
                  name:
                    description: The name of the referenced resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              subject:
                description: |-
                  The identity the authentication provider vouches for: email, sub or
                  username depending on the proxy authentication mode.
                maxLength: 253
                minLength: 1
                type: string
            required:
            - pgConsoleRef
            - roleRef
            - subject
            type: object
          status:
            description: PgToolBoxUserStatus is the observed state of a PgToolBoxUser.
            properties:
              conditions:
                description: 'Standard conditions: Ready, RoleReady, ProxySynced,
                  PgAdminSynced.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: The generation observed by the controller.
                format: int64
                type: integer
              pgAdminSynced:
                description: |-
                  Whether the pgAdmin account and shared server definition are
                  provisioned for this user.
                type: boolean
              proxySynced:
                description: Whether the rendered proxy configuration includes this
                  user.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
